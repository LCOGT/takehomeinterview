export const DowntimeTemplate = (props: {
    downtimeId: React.FC,
    siteId: React.FC,
    telescopeId: React.FC,
    startDate: React.FC,
    endDate: React.FC,
    reason: React.FC,
}) => {
   return (<div>
   </div>);
}